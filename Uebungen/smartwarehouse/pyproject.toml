[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "smartwarehouse"
version = "0.1.0"
authors = [{ name = "Jan" }]
description = "A simple smart warehouse manager exercise"
readme = "README.md"
requires-python = ">=3.10"
